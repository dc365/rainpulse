# 7.3.5 近距离源模型判定

near_range_targets默认false，仅7.3.5允许；旧参数哈希不变。
训练域仍为minimum_range_m（50km）以外。新增目标域为已有r>0近距离门；目标和保护块从参考中排除。近距离数据不进入远距离源功率拟合。
新增近距离门必须通过现有留出功率/SNR/极化匹配和宽角参考；天气、冲突及平台保护保留。近距离禁止长条形态或边缘关联绕过极化检查。不填补缺测、不直接删除整个扇区。BWS_REASON bit256记录近距离目标身份，与匹配/保护原因并列。
