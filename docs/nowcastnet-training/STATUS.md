# NowcastNet 训练阶段状态

更新日期：2026-09-01

方案版本：`TRAINING_PLAN_v1.0`

总体状态：`foundation_first_formal_window_accepted`

当前阶段：阶段 B、首个正式窗口已验收，演化训练到第 243,046 步；第二个窗口尚未批准

下一动作：人工批准绑定第 243,046 步检查点的一次性启动许可，再用约 2.59 GPU 小时完成剩余 56,954 步演化训练

## 1. 阶段看板

| 阶段 | 状态 | 准入条件 | 完成证据 |
|---|---|---|---|
| 路线和边界冻结 | 完成 | 用户确认 MRMS 基础训练与福建微调路线 | v1.0 方案、独立文档入口 |
| 数据准备 | 完成 | 原始 MRMS 只读可访问，试制容量已确认 | 4,000 个 Zarr shard、100,000 个样本均通过内容哈希和随机回读验收；独立留出为 0 |
| 训练器实现 | 完成 | 试制数据通过门槛 | 演化网络、生成器、时空判别器、双优化器、连续训练和严格恢复均已通过 |
| GPU 冒烟 | 完成 | 训练器 CPU/小样本验证通过 | 演化网络 1,000 步、生成阶段批量 1/8/16、最小推理导出及固定向量零误差对照已通过 |
| MRMS 完整训练 | 进行中 | GPU 冒烟、夜间调度演练和检查点晋级预检通过 | 首窗从第 2,819 步到第 243,046 步；剩余 56,954 步，第二窗尚未批准 |
| 开发集评估 | 未开始 | MRMS 最终候选可加载 | 冻结指标、门槛、配置和事件清单 |
| 独立留出评估 | 关闭 | 开发门槛已冻结并哈希 | 正式留出报告，仅运行一次 |
| 福建数据准备 | 等待真实资料 | 完成 v1.1 质控、拼图、QPE 和连续样本链路 | 福建训练、开发、留出清单 |
| 福建微调 | 未开始 | MRMS 父模型通过门槛且福建数据就绪 | 子模型、父权重引用和本地评估 |
| 影子运行 | 关闭 | 福建独立验收通过和人工批准 | 关闭发布的连续影子报告 |
| 业务发布 | 关闭 | 业务评审明确批准 | 发布变更、回滚和监控证据 |

## 2. 当前已确认事实

- MRMS 实际覆盖 2019-01-01 至 2025-12-31，共七个自然年。
- 训练集为 2019 至 2023 年，开发月份为 2024-02、05、08、10，独立留出月份为 2025-02、05、08、10。
- 当前 GPU 为约 84 GB 的 RTX 6000D；暂停已授权的 Qwen3.8 后，预计释放约 38 GB 显存。
- 完整样本只写入 NAS，验收后仍有 836,348,674,048 bytes 可用。本地盘不承载完整样本；checkpoints、指标和推理制品继续保留在服务器本地。
- 官方 NowcastNet 只有可用推理代码和权重，没有可直接采用的完整训练器。
- 官方权重在现有 MRMS 验证中落后于 STEPS，因此自训练模型必须以 STEPS 为主要竞争基线。
- 福建真实数据尚未到位，福建技巧、微调效果和业务资格均不可声明。
- 10,000 样本试制固定使用 2019 至 2023 年各 80 个时间窗。每个时间窗生成 25 个空间样本，其中 20 个按降水重要性抽样，5 个做全域均匀抽样。
- 真实 29 帧 smoke 生成 25 个有效样本，耗时 21.09 秒，峰值内存约 3.0 GiB；逻辑数据 142,540,800 bytes，压缩后 17,863,097 bytes。该步骤只使用 CPU、内存和 NAS I/O，没有停止共享 GPU 服务。
- 10,000 样本试制已完成。400 个 shard 全部通过内容哈希检查，8,000 个重要性样本和 2,000 个均匀样本全部可回读，未读取独立留出数据。
- 试制集逻辑数据为 57,016,320,000 bytes，压缩后为 5,778,298,957 bytes，压缩比约 9.87。固定随机回读 64 个样本的逻辑吞吐为 465.73 MiB/s。
- 论文公开材料只说明 MRMS 宽高各降为一半，没有公开数据预处理所用的重采样核。RainPulse 对照轨道明确采用 2 × 2 面积平均，但不将这一选择表述为官方未公开实现。
- 当前 0.01° 试制样本为 256 × 256，不能直接充当论文的 0.02°、256 × 256 训练样本。独立对照集从 0.01°、512 × 512 原生窗口开始，降尺度后得到 16 个 0.02°、256 × 256 样本，全部内容哈希和回读检查通过。
- RainPulse 演化 U-Net 与审阅过的官方推理实现均为 8,862,548 个参数、161 个参数张量，参数形状多重集完全一致。
- RTX 6000D 上，0.01° 和 0.02° 单批 BF16 前向、反向、优化器更新与检查点回读均已通过。0.01° 批量 16 峰值分配显存约 6.81 GiB、峰值保留显存约 7.80 GiB；批量 8 分别约 3.47 GiB 和 3.96 GiB。
- CUDA `grid_sampler_2d_backward_cuda` 在当前 PyTorch/CUDA 组合中没有确定性实现。检查点恢复要求模型、优化器、调度器和随机状态指纹完全一致；两次独立 CUDA 运行不宣称逐位一致，短程损失轨迹使用绝对 0.05、相对 1% 的冻结容差。
- 演化网络已按批量 16 完成 1,000 步：首进程运行到 500 步后退出，新进程验证状态指纹并恢复到 1,000 步。1,000 行指标连续且全部有限，最终检查点和指标 SHA-256 已登记。
- 1～500 步耗时 117.00 秒，501～1,000 步耗时 111.20 秒，平均每步 0.220 秒，P95 为 0.234 秒；该结果仅用于工程容量估计，不代表模型已收敛或达到技巧门槛。
- RainPulse 生成器与官方 MIT 推理实现的 195 个可训练参数张量和 26,037,288 个可训练参数完全一致。时空判别器按论文 Methods 与 Extended Data Fig. 1c 复现；官方没有发布训练源码，因此该部分明确标记为 RainPulse 复现，不声称取得官方未公开实现。
- 生成阶段已使用 1,000 步演化检查点完成 256 × 256、4 成员、BF16 的真实 GPU 前向、判别器更新、生成器更新和检查点回读。批量 1、8、16 的损失与梯度均为有限值。
- 生成阶段批量 16 的峰值分配显存为 37,913,757,696 bytes，峰值保留为 39,214,645,248 bytes。它在只暂停 Qwen3.8 时可以运行，但共享 GPU 余量过小，尚未批准用于长训练。批量 8 的对应值为 19,406,717,952 bytes 和 20,539,506,688 bytes，只作为阶段 B 安全容量基线，不视为论文全局批量 16 的等价替代。
- 生成冒烟使用的演化网络只有 1,000 步，因而仅证明机械链路；生成阶段 500,000 步训练仍必须等待演化网络完成 300,000 步。
- 最小推理制品只包含演化网络和生成器状态，不含判别器或优化器。使用固定 MRMS 输入和 4 组固定噪声，训练检查点显式链路与全新加载推理链路均输出 `[1,4,20,256,256]`，原始输出和 0～128 mm/h 裁剪输出的最大绝对差均为 0。
- 当前推理导出继承 1,000 步演化和 1 步生成冒烟权重，只证明制品结构、血缘和数值加载一致，不能用于技巧声明或业务发布。
- 生成阶段连续训练已完成短程中断恢复验收：参考运行连续 6 步，对照运行在第 3 步退出并由新进程恢复到第 6 步。检查点完整保存冻结演化权重、生成器、判别器、两个 Adam 和 Python、NumPy、PyTorch、CUDA 随机状态；恢复后所有状态指纹精确一致。
- 第 4～6 步的批次标识完全一致。生成总损失、对抗损失、池化正则和判别器总损失的最大绝对差分别为 0.013615、0.000676、0.000479 和 0.000464，均通过绝对 0.05、相对 1% 的冻结容差。独立 CUDA 运行不声明逐位一致。
- 完整样本库容量盘点已完成。本地训练运行盘可用 402,122,895,360 bytes，低于 500 GB 门槛，明确禁止承载完整库；外部训练制品卷可用 1,033,286,320,128 bytes，通过门槛，超出部分为 533,286,320,128 bytes。
- 按 10,000 样本实测线性外推，100,000 样本逻辑量约 570,163,200,000 bytes，压缩存储约 57,782,989,570 bytes。完整库冻结为启动或恢复前至少 500 GB 可用、逻辑上限 600 GB、物理输出硬上限 400 GB；输出通过外部环境注入，必须与只读原始 MRMS 根目录分离，且不得读取独立留出窗口。
- 完整样本库最终计划为 revision 6，包含 4,000 个互异训练窗口，共生成 100,000 个样本；独立留出窗口为 0。物化期间有 5 个窗口因源文件不是 gzip 或有效空间裁剪不足被确定性替换，每次替换前均核对已完成 shard 身份，旧计划也已保留。
- NAS 真实 shard 冒烟生成 2 个 shard、50 个样本，重要性与均匀抽样为 40/10。逻辑量 285,081,600 bytes，实际 shard 存储 21,850,601 bytes，全部内容哈希通过；固定随机回读 16 个样本的逻辑吞吐为 526.46 MiB/s。
- 审计清单历史上保留 `raw/` 相对路径前缀，首次冒烟因此触发重复根目录拒绝，未写出错误 shard。修正后保持环境变量指向真实 raw 根目录并安全恢复；再次执行复用既有 2 个 shard，0.02 秒完成且样本索引哈希不变。
- 100,000 个完整训练样本已完成物化，共 4,000 个 shard。重要性与均匀抽样为 80,000/20,000，逻辑量 570,163,200,000 bytes，压缩后为 57,250,395,156 bytes，样本索引 SHA-256 为 `e758c938…cddf`。
- 完整库的 4,000 个 shard 已逐一核对内容哈希，固定随机回读 64 个样本，逻辑吞吐为 79.30 MiB/s，抽样雨强范围为 0～128 mm/h。物化与验收均未读取独立留出，也没有启动 GPU 训练或停止 Qwen 及其他服务。
- 正式训练配置已绑定最终 revision 6 计划、完整样本验收证据和样本索引，不再接受最初冒烟计划。配置哈希为 `874775ed…920e8`。
- 105 上的 CPU preflight 已通过。代码、配置、100,000 个样本、4,000 个 shard、最终计划、样本索引、输出盘、数据与输出隔离、共享内存、16 个样本回读及 `new` 检查点边界均符合要求；独立留出读取为 0。
- CPU preflight 只证明正式训练身份与非 GPU 条件合格。该步骤执行时训练输出目录为空，检查前后的 GPU 进程保持不变，Qwen 和其他服务没有停止。
- systemd 首次停止测试发现默认 `KillMode=control-group` 会同时终止 DataLoader workers，主进程因此无法完成训练报告。失败运行已单独保留且没有转为恢复点；训练单元改用 `KillMode=mixed` 后重新从空目录演练。
- 有效 `new` 演练从 0 到 2,096 步，运行 597.28 秒；有效 `resume` 演练由新进程从 2,096 步恢复到 2,819 步，运行 297.55 秒。两次均由 timer 发送 SIGTERM，训练状态为 `stopped`，检查点和 nightly report 哈希通过。
- 恢复前精确核对模型、优化器、调度器及随机状态指纹，输入检查点为 `8267d595…fd5e`，最终演练检查点为 `328a2954…47ab`。两段合计 2,819 步，峰值保留显存约 8.44 GB，独立留出读取为 0。
- 两段有效演练的整体吞吐为 3.15 步/秒，较慢的恢复段为 2.43 步/秒。演化网络 300,000 步预计需要约 26.5～34.3 个 GPU 小时，建议安排 3 个 12 小时窗口并保留第 4 个窗口处理检查点和故障余量。
- systemd 演练结束后没有遗留训练进程，Qwen3.8 已恢复健康并交由服务器侧 user systemd 管理，其他 GPU 服务没有停止。
- 第 2,819 步检查点已获人工批准，晋级时重新核对文件哈希、模型、优化器、调度器、随机状态、完整样本身份和独立留出读取计数。该检查点从正式训练起点计入 300,000 步目标。
- 主工作区加入文档和测试提交后，严格预检按设计拒绝了提交号不同的恢复请求。正式训练改用生成检查点的固定代码提交，复查通过且训练路径保持干净；恢复契约没有放宽。
- 首窗定时器按 Asia/Taipei 时区 20:00 启动、07:45 优雅停止，并设置 12 小时运行上限。启用时训练服务保持未运行，Qwen3.8 健康，其他 GPU 服务没有停止，独立留出为 0。
- 首窗首次启动因请求的 1,800 秒检查点间隔与晋级运行清单冻结的 300 秒不一致而被拒绝，没有新增训练步数，Qwen3.8 自动恢复。配置改回 300 秒后从同一检查点重新启动。
- 有效首窗从第 2,819 步运行到第 243,046 步，完成 240,227 步，平均 6.12 步/秒，峰值保留显存约 8.44 GB。全部 243,046 行累计指标连续且为有限值，最终检查点为 `5b4068d0…dae6`。
- 07:45 已收到 SIGTERM 并完成检查点、训练报告、nightly report 和 Qwen3.8 恢复。`ExecStopPost` 随后因内嵌 `disable --now` 等待当前停止事务，单元在 180 秒后记为 timeout；训练制品与共享服务恢复不受影响。
- 收尾脚本已移除 timer 控制，独立复验在 1 秒内完成。后续每个窗口改用绑定输入检查点的一次性启动许可；当前训练服务和两个 timer 均已关闭，第二窗许可不存在。Qwen3.6 首窗未停止，窗口后健康检查通过。

## 3. 数据准备执行清单

- [x] 再次只读确认 MRMS 根目录、文件命名、产品、单位和时间约定。
- [x] 生成全量资产清单和 SHA-256 策略。
- [x] 生成缺帧、重复帧、损坏文件和不完整日期报告。
- [x] 固化训练、开发、独立留出和后备清单。
- [x] 为加载器增加切分越界拒绝测试。
- [x] 生成连续 29 帧训练和开发窗口索引，保持独立留出索引关闭。
- [x] 校验 0.01° 坐标方向、经度范围、纬度顺序和像元中心。
- [x] 实现缺测拒绝、雨强截断和采样权重。
- [x] 冻结试制集随机种子、时间窗和空间采样规则。
- [x] 冻结 10,000 样本试制容量门槛：本地 NVMe 至少 120 GB 可用，输出硬上限 80 GB。
- [x] 冻结完整样本的 500 GB 容量策略；本地盘不通过，外部训练制品卷有 1,033,286,320,128 bytes 可用并通过门槛。
- [x] 冻结约 100,000 样本的独立配置、确定性时间窗计划和物理输出硬上限守卫。
- [x] 可恢复地物化并验收 100,000 个完整训练样本；4,000 个 shard 已通过全量内容哈希和随机回读。
- [x] 生成 10,000 样本试制分片。
- [x] 完成随机回读、统计分布、吞吐和哈希验证。
- [x] 把完成证据和配置哈希填回本文件，代码提交在本批次完成后补记。

## 4. 阶段 B 执行清单

- [x] 冻结训练运行配置以及 0.01°、0.02° 两条数据轨道。
- [x] 实现带完成标记、样本索引哈希和显式有效掩膜检查的 Zarr 训练加载器。
- [x] 实现演化双路径 U-Net、半拉格朗日演化、加权 L1 和 Sobel 运动正则损失。
- [x] 生成独立 0.02° 协议对照小样本并完成全量回读。
- [x] 完成 0.01°、0.02° 单批 GPU 更新和检查点写后回读。
- [x] 完成 BF16 批量 16、批量 8 的演化网络资源测量。
- [x] 实现连续训练、原子检查点、严格状态恢复和损失轨迹容差对照。
- [x] 完成 1,000 步演化训练，在 500 步退出进程并恢复到 1,000 步。
- [x] 实现生成器、时空判别器、4 成员随机噪声、对抗损失、池化正则和双优化器单步闭环。
- [x] 完成生成阶段 BF16 批量 1、8、16 的 GPU 更新、资源测量和检查点回读。
- [x] 实现生成阶段连续训练、成对优化器边界原子检查点、优雅停止和中断恢复对照。
- [x] 制作最小推理导出并与训练进程的固定输入、固定噪声输出做零误差核对。

## 5. 当前禁止动作

- 不在 20:00 至次日 08:00 窗口外手工启动正式演化训练。
- 不在人工批准前创建第二个窗口的一次性启动许可或启用 timer。
- 不修改固定训练代码提交后继续沿用当前检查点；训练代码变化必须升版并重新验收。
- 不打开独立留出结果。
- 不长期停止 Qwen3.8。
- 不暂停未授权的其他 GPU 服务。
- 不使用 2024、2025 开发和留出月份参与训练。
- 不把缺测填成无雨。
- 不把权重、训练样本、服务器连接信息或密码提交到 Git。
- 不修改 RP-026 结论来替代本训练支线的状态。

## 6. 阻塞与风险

| 项目 | 当前判断 | 处理方式 |
|---|---|---|
| 派生数据容量 | 完整样本已完成 | checkpoints、指标和推理制品保留本地；训练样本只放 NAS，验收后仍有约 836.35 GB 可用 |
| 官方训练器缺失 | 已知 | 在 RainPulse 中实现并用 0.02° 小样本对照 |
| 官方降尺度核未公开 | 已知 | 2 × 2 面积平均作为 RainPulse 明示规则，记录限制，不声称完全复现官方预处理 |
| 0.01° 物理尺度差异 | 已知 | 从随机初始化训练，不直接微调官方 0.02° 权重 |
| 共享 GPU | 演化阶段可管理 | systemd 演练峰值保留显存约 8.44 GB；只暂停已授权的 Qwen3.8，结束后自动恢复并检查健康 |
| 生成阶段批量 16 余量 | 单步可运行，长期余量不足 | 不扩大停服范围；批量 8 仅作阶段 B 安全基线，完整训练前另行批准安全资源窗口或升版训练协议 |
| CUDA 插值反向非确定性 | 已确认 | 状态恢复使用精确指纹；独立运行轨迹使用冻结数值容差，不作逐位一致声明 |
| 福建真实资料 | 尚未到位 | 先完成 MRMS 基础模型，不提前声明本地技巧 |

## 7. 证据登记

完成一个步骤后追加一行。不得只写“完成”，必须提供可复现命令、制品路径或哈希；服务器私有路径用外部配置名表示。

| 日期 | 步骤 | 代码提交 | 配置/数据哈希 | 证据与结论 |
|---|---|---|---|---|
| 2026-08-30 | 训练路线冻结 | 待提交 | `TRAINING_PLAN_v1.0` | MRMS 基础模型、福建子模型、夜间断续训练和验收门槛已形成独立文档 |
| 2026-08-30 | MRMS manifest 全量审计 | 待提交 | profile `c54d200a…618bc`；inventory `249446df…bea3`；windows `856cfc72…3410` | 293.57 秒完成 367,851 帧和 2,557 份 manifest 审计，传输完整性通过；357 个源缺帧保持显式；257,141 个训练窗口和 17,426 个开发窗口已索引；留出 0 行 |
| 2026-08-30 | 试制协议与计划冻结 | 待提交 | pilot profile `2404e6ef…a1cf`；plan `1b28ccf6…fa68` | 400 个训练时间窗覆盖 2019 至 2023 年，每年 80 个；计划生成 10,000 样本，独立留出 0 个 |
| 2026-08-30 | 真实 29 帧试制 smoke | 待提交 | sample index `a50f4477…a926` | 25 个样本全部有效，重要性/均匀抽样为 20/5，Zarr 回读无 NaN/Inf，雨强范围 0～94.125 mm/h；耗时 21.09 秒，峰值内存约 3.0 GiB |
| 2026-08-30 | 10,000 样本试制验收 | `ad245ff` | evidence `nowcastnet-mrms-pilot-v1.json`；sample index `0d1569b1…bdff` | 400 个 shard 和 10,000 样本通过全量内容哈希；8,000/2,000 抽样比例准确，独立留出 0 个；64 样本随机回读吞吐 465.73 MiB/s |
| 2026-08-30 | 0.02° 协议对照集 | 本批次 `main` | run profile `1f8fe804…fb8b7`；sample index `0b54b818…62bf` | 16 个样本均由 0.01°、512 × 512 原生窗口经 2 × 2 面积平均得到；全量 shard 内容哈希通过，雨强范围 0～128 mm/h，独立留出 0 个 |
| 2026-08-30 | 演化结构与双轨单步 GPU 冒烟 | 本批次 `main` | structure evidence `nowcastnet-evolution-structure-conformance-v1.json` | U-Net 参数数量和形状与官方推理实现一致；0.01°、0.02° 均完成 BF16 前向、反向、更新和检查点回读，无 NaN/Inf |
| 2026-08-30 | 演化网络 GPU 资源测量 | 本批次 `main` | sizing evidence `nowcastnet-foundation-evolution-gpu-sizing-v1.json` | 批量 16 和 8 均通过；峰值保留显存约 7.80 GiB 和 3.96 GiB，Qwen3.8 测试后恢复健康 |
| 2026-08-30 | 演化训练恢复语义验收 | `299c618` | resume evidence `nowcastnet-evolution-resume-conformance-v1.json` | 模型、Adam、调度器和随机状态精确恢复；第 4～6 步损失在绝对 0.05、相对 1% 容差内一致；明确记录 CUDA 插值反向非逐位确定 |
| 2026-08-30 | 演化网络 1,000 步中断恢复 | `299c618` | evidence `nowcastnet-evolution-1000-resume-v1.json`；checkpoint `709416cc…c0700`；metrics `0f444873…10890` | 批量 16、BF16；500 步退出后由新进程恢复至 1,000 步，10 个检查点、1,000 行指标全部有效，Qwen3.8 已恢复健康 |
| 2026-08-30 | 生成网络与判别器 GPU 冒烟 | `ae1cb8e` | evidence `nowcastnet-generative-smoke-v1.json`；profile `06ffe74e…9f28` | 官方生成器参数形状完全一致；论文判别器复现完成；批量 1、8、16 的 4 成员 BF16 双优化器更新与检查点回读通过；批量 16 共享余量不足，完整训练资格仍关闭，Qwen3.8 已恢复健康 |
| 2026-08-30 | 最小推理导出与数值对照 | `6b510f6` | evidence `nowcastnet-inference-export-v1.json`；manifest `a8e7b423…587e5` | 仅导出演化与生成状态；固定输入和 4 组固定噪声下，全新加载输出与训练检查点显式链路原始值、裁剪值均零误差一致；当前冒烟权重不具技巧或业务资格 |
| 2026-08-30 | 生成阶段连续训练与中断恢复 | `cb23541`、`59318c5` | evidence `nowcastnet-generative-resume-conformance-v1.json`；resume checkpoint `ca6fac07…2e7c` | 第 3 步退出后由新进程恢复到第 6 步；全模型、双 Adam 和随机状态指纹精确恢复，第 4～6 步四项损失通过冻结容差；仍使用 1,000 步演化权重，仅具阶段 B 机械验收资格 |
| 2026-08-30 | 完整样本库容量门槛 | `4f567f1` | evidence `nowcastnet-full-sample-capacity-v1.json` | 本地盘 402.12 GB 可用，不通过；外部训练制品卷 1.033 TB 可用，通过 500 GB 门槛。冻结逻辑 600 GB、物理 400 GB 上限，完整物化尚未开始 |
| 2026-08-31 | 完整样本计划与真实 shard 冒烟 | `7f4fe9b`、`44fa0f6` | profile `c84c76c3…34be5`；plan `8cc86245…c1f2`；sample index `614ddb8f…2fd9` | 4,000 个训练窗口覆盖 2019 至 2023 年且独立留出为 0；NAS 上 2 个 shard、50 个样本通过内容哈希和随机回读，续跑复用与容量硬门槛均通过，完整物化可以开始 |
| 2026-09-01 | 100,000 个完整训练样本验收 | 本批次 `main` | evidence `nowcastnet-full-sample-library-v1.json`；plan `5ce3859f…6892`；sample index `e758c938…cddf` | 4,000 个 shard 和 100,000 个样本通过全量内容哈希；80,000/20,000 抽样比例准确，独立留出为 0；64 个样本随机回读吞吐 79.30 MiB/s，GPU 完整训练仍未启动 |
| 2026-09-01 | 正式训练 CPU preflight | `125f68c` | evidence `nowcastnet-foundation-cpu-preflight-v1.json`；preflight `e9fabcdc…4cda`；profile `874775ed…920e8` | 代码与训练树干净，最终计划和 100,000 个样本身份一致，16 个样本回读、存储隔离、磁盘、共享内存和 `new` 边界均通过；设备为 CPU，GPU 训练未启动，下一门槛为 30 分钟 systemd 演练 |
| 2026-09-01 | systemd 启停恢复演练 | 本批次 `main` | evidence `nowcastnet-foundation-systemd-rehearsal-v1.json`；checkpoint `328a2954…47ab`；metrics `ab81463a…f093` | 修正 `KillMode` 后，`new` 运行到 2,096 步并由新进程恢复到 2,819 步；两次 SIGTERM 停止、状态恢复、检查点哈希和报告均通过，Qwen3.8 已恢复健康；演练检查点尚未转为正式训练起点 |
| 2026-09-01 | 正式训练起点批准与夜间调度 | 本批次 `main` | evidence `nowcastnet-foundation-formal-training-approval-v1.json`；checkpoint `328a2954…47ab`；preflight `d68f7b7e…bd98` | 第 2,819 步检查点已转为正式起点；固定训练代码提交的恢复预检通过，20:00 启动和 07:45 停止 timer 已启用；首窗后自动暂停下一次启动，训练尚未开始，独立留出为 0 |
| 2026-09-02 | 首个正式演化训练窗口 | 本批次 `main` | evidence `nowcastnet-foundation-first-formal-window-v1.json`；checkpoint `5b4068d0…dae6`；metrics `3251e6dc…3589` | 有效窗口从第 2,819 步到第 243,046 步，240,227 步指标连续且有限，检查点和 nightly report 通过；Qwen3.8 已恢复，停机嵌套调用已修正，第二窗保持关闭 |
