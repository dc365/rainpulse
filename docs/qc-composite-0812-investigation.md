# 2026-08-28 08:12 CST 组合反射率残留核查

本次核查使用 105 当前 Z9598 的 qc-opensource-7.3.6 QC 产物。
统计区域为原始极坐标方位 180–310 度、斜距大于 100 km；它是截图红框的近似方向窗口，并不是逐像素归因结果。统计数不能当作确认污染门数。

| 仰角 | 有限 DBZH_QC 门 | VALID 且 QPE eligible 门 | eligible 中 RV2_LINE 候选 |
| --- | ---: | ---: | ---: |
| 0.50° | 3492 | 448 | 未统计 |
| 1.47° | 38800 | 147 | 未统计 |
| 2.39° | 57286 | 9466 | 1001 |
| 3.36° | 45911 | 7300 | 625 |
| 4.28° | 46996 | 4867 | 221 |
| 5.99° | 9566 | 2307 | 未统计 |

2.39°、3.36°、4.28° 上这些 eligible 门中，RV2_ACTION_PROPOSAL、RV2_QUALIFIED、SRC_REVIEW_WEATHER_PROTECTED、SRC_REVIEW_CONFLICT、RV2_PLATEAU、V6_WEATHER_PROTECTED 均为零。部分形态候选未达到处置条件；不能归因于版本漏掩膜或天气保护。

## 实算结果

- 对这三个仰角重新运行现有 variable_tracks，新增可隔离 eligible 门为零。
- 试验长距离功率一致性：50 km 外、至少 40 门、跨度 60 km、相邻片段间隔不超过 10 km；85% 门的距离订正功率在中位数 ±2 dB 内，并要求两半独立中位数差小于 1 dB。新增命中也为零，未接入运行逻辑。
- 当前生产没有部署新的射线算法，也没有将这次统计窗口直接做成删除区域。

## 尚未完成

1. 在多站、多仰角最大值合成时保存每个像素的胜出站、仰角、射线与距离门，准确归因红框。
2. 对胜出门检查形态候选不满足隔离条件的具体分支，围绕该分支实施修复。
3. 用相同输入输出前后对照图；分别统计可疑区域变化和天气区域误删，再部署。

目前只有逐层排查证据，不能声称红框已经改善，也不能把所有高层 eligible 门当成异常。

## 合成像素追踪补充

已实现 `composite_reflectivity(..., return_sources=True)`，每个有效像素返回实际胜出的雷达序号、仰角编号、射线和距离门，缺测用 -1。默认返回和数值合成行为保持不变。

105 四站数据重新合成后，在 113–118.4°E、24–29°N 的矩形核查窗（包含红框及周边）中，Z9598 的 sweep 4/5/6 分别胜出 11939/8526/6338 个像素，对应 2.39°/3.36°/4.28°。这三层合计 26803，约占核查窗有效像素的 61.4%。因此高仰角回波确实进入合成，而不是仅仅存在于输入数据。窗口中还含其他站的贡献，不能把整个窗口视作污染。

新追踪功能的自动测试覆盖最大值来源、源门值还原、隔离后来源切换、缺测来源标记。生产质控尚未变更。

## 2026-09-19 实施与同输入对照

前述“尚未完成”为上一轮状态。本轮完成候选分支检查、宽扇区规则接入和诊断重放；生产发布仍未执行。

原因：高仰角多射线成束，细线限制（最多数度、两侧逐门强差）排除了大部分残留。
2.39°层1001个残留细线候选中仅325个有双侧测量对比，73个达到6 dB，
不足以通过原有长距离强边界条件。原始扇区的距离分块中位数却保持稳定的
随距离增强规律，逐门噪声较大，因此改用分块统计结合扇区边界。

新增 `power_fan.py`；独立开关与子配置，输出实际原始门的形态隔离提案，经原有
source review 决策链进入数值 QC。没有按站号、日期、方位窗口硬编码删除。
核查窗只用于统计，不输入算法。

105上读取四站、所有仰角，使用真实 native adapter、天气保护/冲突/数值平台屏障，
在内存中将新命中门从现有 eligibility 中排除，再重新取所有站/仰角的最大值：

- Z9598 新命中原 eligible 门：sweep 2/4/5/6 = 1720/16142/7883/6945；最低层0。
- Z9591 sweep 0/2 = 738/15；Z9593、Z9599新增为0。
- 113–118.4°E、24–29°N核查框有效像素43659→28566，减少15093（34.6%）。
- 框内16429个像素降低或变成无有效来源；仍有其他层/其他站和未达规则的残留。
- 这是增量数值掩膜诊断重放，不是完整 worker 重算，不写回对象库、不发布网页。
- 形态规则并非真值标签，不能把34.6%说成准确率，也尚未证明真实天气零误删。

复现工具：`scripts/replay_qc_power_fan.py`，参数 `--qc-uri`（可多次）、
`--profile configs/qc/radial-20260918/radial-power-fan-20260919.yaml`、
`--radar-config-dir`、`--output /tmp/comparison.npz`；由环境提供对象存储连接。
运行需 `PYTHONPATH=algorithms`。工具不包含站点URI或账号密钥。

对照图（本地私有，不入库）：`artifacts/qc-power-fan-20260919/composite-0812-before-after.png`。

验证：径向修订、review extension、组合反射率共197项测试通过；子配置通过
`load_open_source_profile` 校验。新增测试先因功能缺失失败，实现后通过，覆盖
宽噪声扇区、恒定/弯曲天气剖面、缺测、几何断点、天气保护、完整角向场、
动作提案、audit无动作与诊断字段校验。代码当前未提交，生产服务未替换。
