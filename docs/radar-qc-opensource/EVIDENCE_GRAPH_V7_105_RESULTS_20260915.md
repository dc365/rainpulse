# V7：105 测试部署与首轮结果

2026-09-15，代码 `76e60c4` 合入 main；真实数据暴露的完成事件超限由 `bebb660` 修复，均已推送。105 实际运行镜像 `qc-opensource-7.0.0-r1`，算法版本 `qc-opensource-7.0.0`。原生 Go 服务和 14 个 QC/Grid/Mosaic/QPE/Diagnostics Worker 协调切换。既有原始数据不变，测试候选仍为 `operational_eligible=false`。

## 集成及运行问题

交付包是未完成工作树，不是完整 V7 验收版。除了应用 17 个文件，还补齐 V7 输出验证：分开校验旧阶段基线和 V7 新增动作，保留原始有效性、缺测与定量资格边界。真实 Worker 合成测试验证了序列化路径。

首轮旧镜像 20 个 QC 任务中，17 个成功、3 个在 `result_publish` 阶段报 `nats: maximum payload exceeded`。计算及对象上传已完成，失败原因是 V7 图节点明细进入完成事件。修订仅将事件改为有界标量摘要及 `qc/summary.json` 定位，完整明细仍存于可校验的 QC 资产，不提高 NATS 上限，不改变气象判定。

快速验证：51 项相关 Python 测试、Go API/workflow 测试、Web build、BDP Linux Go build 通过；事件修复后 16 项针对性测试通过。未执行全项目验收，原生 bRopo Emitter/RDD 未启用。

## 首轮真实结果

修订后 08:45 起报任务 `a1e7c7b8-786e-511c-9e4b-d04af28ae563` 已 `SUCCEEDED / PUBLISHED`，耗时 1072 秒（约 17.9 分钟）。20 QC、20 Grid、6 Mosaic、6 QPE、6 Diagnostics 全部成功。08:50 已由后台脚本接续，随后安排 08:30；该状态是记录时的快照，不代表全部后续重算完成。

已检查 08:30、08:40、08:45 的四站诊断。每张原始/质控 PNG 都由 API 元数据确认 `qc_pipeline_version=qc-opensource-7.0.0`，并逐一验证 PNG SHA256。新旧体扫和仰角一致，12 张原始 PNG 与前次冻结材料逐字节一致；诊断采样和色标未变。

相对上一轮已发布 V6.1，640×640 质控图变化如下（均为少量显示像素移除，不是门数或准确率）：

|北京时间|Z9591|Z9593|Z9598|Z9599|
|---|---:|---:|---:|---:|
|08:30|8|0|1|2|
|08:40|38|0|9|0|
|08:45|38|0|83|0|

**主要残留没有得到明显改善。** Z9591 西向强细线仍在，Z9598 多方向径向碎片依然明显，Z9599 的残余细线在检查时次几乎不变；Z9593 主体图像保持不变。不能将源码集成和任务成功表述为算法效果通过验收。

V7 模块确实执行：Z9591 08:40 对应体扫 `5a8d4031-5780-5196-89b1-47e79b648c3b` 的已收集 V7 QC 摘要样本中，最低仰角审计显示，RESIDUAL 阶段拒绝 13,725 门，GRAPH 后 13,808 门，隔离数均为 54,586。该 83 门差异是本轮图仲裁的增量，不是 V6.1→V7 全量差值。08:40/08:45 的该站共用同一体扫和同一 V7 QC 资产，不算两个独立样本。

## 后续研究边界

应围绕残留门的 V7 提议、短段/短组过滤、双侧可用性、独立测量证据和最后动作逐级定位；不能仅靠降低全局阈值来追求删得更多。该包仍以旧阶段决定为基线增加保守仲裁，不能当成已完成的通用径向质控重设计。新旧上下文未严格冻结，图片差异不能全部归因于某一模块。静态地物/海杂波资源仍未具备完整验证。

## 材料位置

本地对照图与 105 运行记录分别位于各自项目的 `runtime/reports/qc-evidence-v7-20260915/`。

- `0830/0840/0845_四站_V61与V7对照.png`：每行一站，每行左原始、中 V6.1、右 V7。
- `after/*-diagnostics.json`、`*-comparison.json`：图层资产绑定及逐图对比。
- `refresh-r1.log`、`rerun-r1-*.json`、`status-r1.py`：后台续算状态。
- `stage/rollback/`：受限回退材料；不提交环境配置或凭据。
