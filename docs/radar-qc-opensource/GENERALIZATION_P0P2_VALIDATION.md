# P0–P2 验证范围

基线c3e768，真实git bundle克隆与完整根树核验；未覆盖用户main。
本轮新增代码直接进入新7.2实际QC Runner，不是独立概念脚本。

## 真实九例的关键阶段回放

所有normalized数据文件摘要和QC输入摘要校验；原始DBZH、旧距离模型候选、旧OC1输出逐门核对。
旧健康策略反事实的资格精确复现，再对相同实际上下文数组运行新模块。

|例|站别|新增隔离（最低切面全部门）|行政政策释放资格（全部门）|选定复核域变化|
|---|---|---:|---:|---|
|CASE-0001|SITE_D|0|20652|仅作为范围对照，不赋天气真值|
|CASE-0002|SITE_A|61183|0|高值复核域104640门，剩43457门仍可用|
|CASE-0003|SITE_C|0|62326|小块复核域1282门，释放919，实际可用1062|
|CASE-0004|SITE_B|0|5419|不能把全站当正确天气标签|
|CASE-0005|SITE_D|0|19865|同上|
|CASE-0006|SITE_A|63657|0|高值复核域189171门，剩125514门仍可用|
|CASE-0007|SITE_C|0|67282|小块复核域1368门，释放952，实际可用1134|
|CASE-0008|SITE_D|0|18777|同上|
|CASE-0009|SITE_B|0|5339|同上|

两例SITE_A继续保留NOISE_OUT_OF_RANGE物理降权，预算标为需要人工复核；未强行把剩余扇区清空。
所有新增确认污染=0。SITE_C原先REJECT/QUARANTINE不恢复，919/952只是政策性资格释放，非验证天气。
详细图与数组随交付包，不在Git提交数据。

## 最终实际工程测试

- Python算法/配置/契约全量：**1062通过，3跳过，0失败/错误**，共1065项；约250.6秒。
  三项跳过均为环境未提供原生Emitter core，不算原生算法验证成功。
- 31项新增P0–P2专项全部通过，并加入1项7.2下游完整链路参数化用例；都包含在上述全量中。
- 改动Python文件Ruff检查通过。Go/Web未改动、未在本轮执行；Compose语法和Dockerfile结构校验
  通过，但没有构建镜像或访问105。
- 九例全部通过本脚本的冻结候选/OC1/原始数值/资格一致性断言；输出45张同采样图和全部增量数组。

下列是专项覆盖点：

- 新真实库Worker重复执行产物字节一致，7.2 Zarr字段及分阶段基线校验。
- 新版本加入既有QC→Hybrid→Grid→Mosaic→QPE集成参数化用例。
- 行政单原因、物理噪声、通道缺失/未知、其它未知原因、旧拒绝/隔离保留。
- 12/89/161/300°支持路由，宽形状不能抹去测量内点，但原V5掩码不改。
- 无独立源/距离模型不符/矩不可靠/参考不足/独立天气存在时不得自动隔离。
- 缺测不新增；预算溢出只保留原合格提议，输出人工复核状态；独立天气仍保护。
- 关闭所有新增行为时旧动作、QI、flags不变；旧QC YAML和parameters_hash冻结校验。
- 完成事件不嵌入巨大对象列表，保留有界review_required和summary路径。
- 验证器拒绝没有原始候选支持或无观测门上的新增隔离。

上游原始源码的两个旧参数摘要测试已单独复现失败：它们的手算输入没有排除既有的空扩展
和关闭状态的context字段。本次只修正测试辅助逻辑；不改变生产parameters_hash来迎合测试，
另有完整上游冻结配置摘要作为独立检查。

没有修改Go/React/renderer或预测模型；此轮不把旧Go/Web验证记录借用为本轮已执行。
全项目Ruff存在原有未整理文件；交付修改的Python文件单独通过Ruff检查。
