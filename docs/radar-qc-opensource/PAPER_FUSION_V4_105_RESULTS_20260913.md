# AFL / Paper Fusion V4：105 部署与实际效果

**部署与两组重算已完成；08:30 南向强条带明显改善，但整体径向质控效果仍不通过验收。** 08:15 改善有限，08:35–08:45 多方向条带及远端残留仍在。

## 交付范围

2026-09-13 按用户要求快速整合 `RainPulse_QC_AFL_RDD_Fusion_V4_20260913`，在隔离工作树应用校验过 SHA256 的补丁，37 个改动文件与交付快照一致，然后快进合并到本地 main。代码提交：`9fdff7e784cb4edae4664b36c2a2cfd5e221c5bb`；本次未推送远端。

本版实际运行 V3 基线、AFL 全径向/AFL-local 和保守增量融合。RDD 是外部参考导入接口，现场没有提供参考，未运行原生 RDD。AFL 图示断点及部分阈值是显式工程近似，不是 SWAN 官方实现。保持 `operational_eligible=false`。

## 部署身份

- 镜像：`rainpulse-cpu-worker:qc-opensource-4.0.0`，ID `sha256:80afa3117bc84fa9107c49bfde2f446283963b61f1316df39861b21e816f6bfb`。
- 配置：`configs/qc/fujian-qc-paper-fusion-v4.yaml`，SHA256 `4296b53107fc693ef4f9255a24626e653a487a8ed27b43e870c1f5159de6ecbe`。
- 实际 Worker 中 `paper_fusion.py` SHA256 与本地相同：`607bf3e775a66c5e19f2df1a3c01cf4a0227542b18fe735809b37cb3fd315878`。
- 复用 Py-ART 2.2.5 / wradlib 2.9.5 依赖镜像；前端构建 `index-BEFMZExu.js`。
- 保留前次原生 Go 控制程序及心跳修复，仅同步其 V4 配置路径。切换前排空相关队列，协调切换 QC/Grid/Mosaic/QPE/Diagnostics（4/4/2/2/2）。
- 服务器回退材料：`runtime/reports/qc-paper-fusion-v4-20260913/stage/rollback`；回退需同步恢复规划器环境与 V3 Worker 覆盖配置。原始雷达资料未改动。

## 验证范围

本次实际执行 60 项相关 Python 测试、5 项 QCReview 前端测试及 TypeScript/Vite 生产构建，全部通过。Python 有一条 numpy 二进制大小兼容性 warning，相关真实库调用测试仍通过。没有重复跑全项目审计。

V3/V4 YAML 除版本身份和 literature 块以外的参数一致。此次是实际任务重新规划后的同案例对照；独立离线工具中的“冻结同一 V3 上下文”实验并未另跑，不能将现场差分等同于严格冻结上下文实验。

## 重算记录与效果

通过现有全链路重算 API 在服务器后台顺序执行；每组完成即发布。08:30 run_id 为 `b6b0040d-eb99-5fc4-8673-08bffdea5dd2`，08:45 run_id 为 `79617d0b-7e32-5a3a-b8ec-1b9d8756c8b1`。最终状态、统计和图像见下方完成记录。

一个实际 QC 任务的核心耗时 7.9 秒，上下文准备 36.7 秒、上传 27.7 秒、端到端 75.6 秒。这只是单样本，不是平均值或 P95，也不能用总重算耗时代表算法核心耗时。

本地证据目录：`runtime/reports/qc-paper-fusion-v4-20260913/`。包含 before/after 实际 API/PNG、校验过摘要 SHA 的 Z9598 体扫统计、comparison.png、部署回执及后台脚本。运行产物不提交 Git。

## 最终完成记录

|北京时间起报|重算状态 / 发布状态|耗时|
|---|---|---:|
|08:30|SUCCEEDED / PUBLISHED|541.7 秒|
|08:45|SUCCEEDED / PUBLISHED|608.5 秒|

合计约 19.2 分钟：38 次 QC、38 次 Grid、各 11 次 Mosaic/QPE/Diagnostics 全部成功。执行次数不是独立样本数。最终原生服务健康，NRestarts=0，14 个相关 Worker 均 healthy。

七个检查时次的 analysis_id 均已更新，原始反射率 PNG 前后逐字节一致。08:25/08:30 共用一个物理体扫，不能重复计为两个独立样本。

以下门数来自 Z9598 全体扫全部切面；不是最低仰角图像像素数或真值检出率。新增门数来自 V4 内嵌基线的增量字段，不直接等于两次独立重算的总门数差。区域覆盖率是多站 QPE 指标，不是单站降水保留率。

|时次|V4 确认径向门|V4 隔离门|融合新增确认门|融合新增隔离门|区域覆盖率 V3 → V4|
|---|---:|---:|---:|---:|---|
|08:10|216,282|66,546|7,946|2,641|45.015% → 45.015%|
|08:15|96,328|19,624|2,677|620|45.201% → 45.191%|
|08:25|98,149|38,633|2,435|3,983|44.887% → 44.887%|
|08:30|98,149|38,633|2,435|3,983|44.219% → 44.219%|
|08:35|200,935|69,749|20,777|7,323|48.356% → 48.356%|
|08:40|299,608|93,722|12,410|11,646|47.605% → 47.605%|
|08:45|293,056|84,338|17,411|6,264|47.352% → 47.287%|

实图检查：08:30 南向连续黄色强条带大幅削弱，剩余零散门和东向条带；08:15 的东/东南向及南向边缘仍明显；08:35 西南向扇形和远端强线段仍在；08:40/08:45 多方向长条带仍在，新增处理未消除主要残留。

建议测试人员在历史案例 2026-08-28 选择 Z9598，核对上述时次，重点检查 08:30 强条带减少是否伴随真实降水误删，以及 08:35–08:45 残留与邻站回波是否一致。没有独立标签和雨量站验证，不宣称召回率或误删率改善。

后续优化优先提取残留门的 AFL/AFL-local 可用性、12 km 连续结构条件、双偏振可靠性和天气保护原因，判断候选未进入还是独立证据不足；远端强线段需逐门核查，不能凭图断定原因。保持 V4 参数冻结供当前测试，不继续盲目放宽全局阈值。
