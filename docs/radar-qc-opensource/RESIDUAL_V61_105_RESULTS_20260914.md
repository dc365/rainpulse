# V6.1：105 部署及首轮实测

2026-09-14 按用户要求快速合并、部署并重算。交付代码提交 `414d241`；现场资源挂载补充 `2e522e3`，均已推送 main。此次完成的是测试部署，不是业务效果验收。

## 合并与部署

交付包 31 个文件通过应用前后摘要校验，应用树与交付树一致。实际运行 57 项相关 Python 测试全部通过，包含真实库调用及下游交付路径；有一条 numpy 二进制大小警告。未重复执行全项目验证。

现场发现原 QC 容器未设置雷达配置和地形资源环境，只有 Grid 配置了这些资源。因此在 V6.1 Compose 覆盖文件中补齐同源的雷达配置目录、地形配置、地形目录和只读挂载。若不补齐，仅修改加载白名单仍不能让现场 QC 使用这些资源。

使用镜像 `rainpulse-cpu-worker:qc-opensource-6.1.0`，协调切换 QC/Grid/Mosaic/QPE/Diagnostics 共 14 个 Worker 与原生规划器；QC 配置为 `fujian-qc-residual-v61.yaml`。诊断仍用 renderer 1.2.0。部署前相关队列为空，部署健康检查通过。保留 V6 镜像及回退材料。

实际 QC 摘要显示 `resources_loaded`、`beam_status=loaded`、`terrain_status=manifest_verified`。高程基准仍为 `unverified_engineering`；DEM 清单校验不代表所有地形块均已使用或气象几何已获独立验收。

## 重算与效果

先运行 08:45 起报以覆盖 08:40，再后台接续 08:30。首轮状态 `SUCCEEDED / PUBLISHED`，约 776 秒；20 次 QC、20 次 Grid、各 6 次 Mosaic/QPE/Diagnostics 全部成功。记录时第二轮 08:30 已进入 QC 阶段，未声称全部历史重算完成。

实际 API 返回的新分析：

|北京时间|analysis_id|观察|
|---|---|---|
|08:35|cff256a6-1d5b-5e20-813d-8de02a0f8345|新诊断已发布|
|08:40|8b8be6c1-0f31-55bc-a022-c374e5489bfa|Z9591 北向、西向红色强线消失；Z9598 部分径向段减少，仍有残留|
|08:45|75098a62-0d34-52c0-a900-2e1b6d0ae843|Z9591 强线仍在；Z9598 多方向径向条带仍明显|

三时次、两站的原始反射率 PNG 前后均逐字节一致，渲染版本均为 1.2.0。因此没有混入上轮 1.1→1.2 的渲染版本变化。但上下文及发布资产链未做严格冻结同输入实验，仍不能将全部差分归因于某一模块。

Z9591 的 08:40 与 08:45 共用体扫 `5a8d4031-5780-5196-89b1-47e79b648c3b`，新发布图像却有强线保留差异。该差异已观察到，但未定位根因；下一步需将两张实际图层逐一绑定到 QC 资产、上下文、仰角和最终资格掩码，避免将错误关联或不同上下文误认为稳定算法收益。同一体扫不得当成两个独立样本。

08:40 两站的实际 QC 摘要均为 `qc-opensource-6.1.0` 且资源加载成功。相对本次运行内嵌 V5 基线，Z9591 新增算法确认 10,474 门、隔离 9,535 门；Z9598 分别为 46,891、12,235 门。这些不是 V6→V6.1 净增量，也不是真值检出率。

一个真实任务核心 10.62 秒、上下文 29.38 秒、上传 35.61 秒、端到端 79.65 秒；单样本不代表平均值或 P95。

## 证据与后续

服务器和本地 `runtime/reports/qc-residual-v61-20260914/` 保存部署脚本、回执、before/after PNG、API 响应、QC 摘要；服务器 `refresh.log` / `status.py` 提供后台重算状态，`stage/rollback` 保存受限回退材料。原始资料未修改，运行产物不提交。

目前是局部改善，尚不能宣布稳定解决或通过验收。优先检查同体扫不同输出的原因、08:45 残留门的候选与保护判据，以及消失的孤立片段是否包含真实降水。保持业务准入未通过。
