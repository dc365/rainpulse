# V6 105 部署与首轮实测（2026-09-14）

代码 `0614a625e09e231df6293c1e64886f338424161b` 已合入并推送 main，105 已部署。首轮 08:30（北京时间）重算成功并发布，08:45 由同一后台脚本顺序接续；此记录不表示全部历史时次完成。

## 部署和验证

- 交付包 38 个路径应用前后 SHA 校验通过，暂存树与交付树一致。
- 本次执行 V6 专项 48 项测试、QCReview 页面 9 项测试及前端生产构建，全部通过。Python 有一条既有 numpy 二进制大小警告。
- 镜像 `rainpulse-cpu-worker:qc-opensource-6.0.0`；QC/Grid/Mosaic/QPE/Diagnostics 共 14 个 Worker，部署健康检查通过。
- 原生规划器同步选择 `configs/qc/fujian-qc-residual-v6.yaml` 和 `configs/diagnostics/qc-residual-diagnostics-v6.yaml`。
- QC 配置 SHA256：`8c51f5c3091be0f5c22f140af471a8fe320af40eb62004a0af61e9bc08a4f291`。
- 诊断配置 SHA256：`97f2f4a9ceea1c6179dc8e033348481b6313a9a6857ea3c7fbc67713c9c337f9`。
- 切换前相关队列已排空，保留 V5 镜像和回退材料，原始资料未修改。

## 首轮实际结果

08:30 target run：`856ae123-5cbb-53a7-a679-ac67fdf0516c`，状态 `SUCCEEDED / PUBLISHED`，约 665 秒。18 次 QC、18 次 Grid、各 5 次 Mosaic/QPE/Diagnostics 全部成功。

一个 QC 任务：核心 12.13 秒、上下文准备 37.04 秒、上传 37.30 秒、端到端 90.67 秒。仅是单样本，不能视为均值或 P95。

首批读取的 Z9591/Z9598 六个体扫摘要均为 `qc-opensource-6.0.0`，各含窄线、范围关联、外围复核和碎片模块记录；相对内嵌 V5 基线，每个体扫新增算法确认 9,573–22,828 门，新增隔离 3,853–7,896 门。这是全体扫工程统计，不是最低仰角像素数、真值检出率或误删率。

实际 API 返回的 08:10、08:15 analysis_id 已切换。检查 08:15 前后 QC PNG：部分孤立碎片减少；Z9591 西南条带、南侧远端片段，及 Z9598 东/东南/南向径向线仍明显。不能宣称径向问题解决或效果验收通过。

诊断由旧版切换至 renderer 1.2.0，原始 PNG 也发生变化；这是采样版本变更，不能把显示差异全部计为质控收益。严格效果量化还需相同渲染规则、相同输入上下文及独立人工标签。特别应检查消失的孤立回波是否包含真实弱降水。

## 后台任务和复核入口

服务器项目目录下：`runtime/reports/qc-residual-v6-20260914/`。`refresh.log` 记录顺序重算，`status.py` 查询状态，`stage/rollback` 保存受限回退材料。本地同名目录保存 before/after PNG、API 响应、摘要、部署脚本和回执，运行产物不提交 Git。

测试人员可在历史案例 2026-08-28 选择 Z9591/Z9598，检查 08:10、08:15、08:25、08:30；08:35–08:45 待第二轮发布后复核。后续优化应针对残留对象的候选、天气保护和最终资格证据定位，不直接放宽全局阈值。

此前 V5 全量批次在 08:50 因贡献雷达仅 1 个、要求至少 2 个而停止；本次没有降低该门槛，也没有宣称 114 个历史时次全量完成。保持 `operational_eligible=false`。
