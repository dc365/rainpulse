# RFI Objects V2 合并与部署状态（2026-09-13）

已读取交付包的实现与运行说明和 README_交给Codex，使用 apply_patch.py 在基线 e150cc90 的独立工作区验证；28 个交付文件与目标树 95af4677c64c6cc41de18e1f6005cd312cf9fefb 一致。

## 合并调整

保留之前门级上下文修复：context.py 不再将不可信门压缩为整条射线；qc_geometry.py 接受门级掩码并兼容旧射线掩码，非法形状直接报错。同步保留对应回归及之前部署记录。交付目录和压缩包不加入源码提交。

## 本次验证

- 完整 algorithms/tests 通过（有既有第三方库警告及一项跳过）。
- Go 全部包测试通过。
- 前端 19 文件、74 项测试通过，TypeScript/Vite 生产构建通过。
- 修改涉及的 QC 引擎、Zarr 和新增对象测试 Ruff 检查通过。
- 对象判定、过去上下文、隔离资格传播及离线回放边界已审核，未发现新的高置信阻断项。

## 105 状态

首次连接因网络超时中断；用户恢复网络后，于 2026-09-13 完成部署。五类 worker 使用 qc-opensource-2.0.0，原生 Go planner 已切换相同 V2 配置，前端 V2 bundle 已核验。真实重算及效果记录见 RFI_OBJECTS_V2_105_RESULTS_20260913.md。

恢复连通后先读服务器当前配置、镜像、作业和队列状态，排空旧任务，保留回退文件及旧镜像；统一切换五类 worker 与原生 Go 的 RAINPULSE_PIPELINE_QC_CONFIG。已有 Go 源码未修改，前端为磁盘 dist，需同步新的 dist。不得直接运行面向旧 evidence-v3 的 deploy_qc_z9598.sh。

先重算一个完整时次，验证 QC profile/参数摘要/下游工件版本一致，再顺序重算剩余案例。分别统计确认剔除、隔离、定量覆盖损失，读取 API 返回的最新诊断资产做前后对照。08:25/08:30 若复用同一物理扫描必须去重，不能作为两个独立效果样本。

参数 temporal_minimum_fraction=0.67：三个过去样本的两票不达阈值，需三票；两个样本需两票。这是当前配置行为，非两票即确认。全部维持 operational_eligible=false，实测结果不能代替独立标注验收。
