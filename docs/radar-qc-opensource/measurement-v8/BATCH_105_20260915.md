# V8 真实数据提取与后台多时次提交

2026-09-15，105 后台容器 `rainpulse-v8-real-20260915` 开始顺序运行
`scripts/qc_measurement_v8_batch.py`，限制 2 CPU / 6 GiB。

选取 2026-08-28 UTC 00:05–00:50 四站已有 V7 QC 的 29 个独立扫描，
覆盖北京时间 08:10–08:50 附近的扫描；各扫描仅处理 sweep_000。
输入通过 ArtifactObjectReader 校验并冻结原始归一化数据及对应 V7 QC，
运行原生 bRopo 检测及 V8 特征提取。没有真实模型，不执行推断、不发布线上产品。
确认前两个扫描成功、第三个处于 extracting 后停止监看。

服务器输出：`runtime/reports/qc-measurement-v8-real-20260915/`。
`inputs.json` 保存扫描与输入 URI；`results/progress.json` 保存进度和逐项状态；
每个扫描保留冻结数据、case.json、features 或 error.log。重跑相同命令会跳过
已有成功 features 的扫描；失败独立记录并继续后续扫描。

容器内复现命令（使用已配置的对象存储环境）：

```sh
python -u /batch.py --manifest /out/inputs.json --output /out/results
```

后台“数据重算”增加追加时次复选框。复用已有 Go rerun API，逐项提交，
分别显示受理/失败和任务状态入口；一个时次失败不阻止后续提交。
须等待所有提交回执后再关闭页面；已受理任务持久化于现有控制平面并由
已有有界 Worker 消费。此入口使用当前线上算法（仍为 V7），不自动启用 V8。
本次没有增加 REST 契约，也不由 Go 启动 Python。
