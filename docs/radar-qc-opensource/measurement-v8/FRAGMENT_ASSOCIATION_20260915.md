# 断续径向对象关联与仲裁：第二步

日期：2026-09-15。代码为默认关闭实验，未切换线上 Worker，也未全站发布重算。

## 本步完成与未完成

完成同一真实径向上的有界关联：从基线已确认 RFI 段，或明确的 V5 距离模型取固定锚点；
锚段至少有 2 km 实测支持。默认关闭，试验设置最大距离 4 km。弱片段与原始锚点需满足
反射率差约束（增长模型按原始距离变化计算），记录对象 ID、原始父射线/门及距离/偏差。
不往缺测门填值，不将其他径向或新片段用作再次扩张的锚点。

关联与动作已分开：每个目标门还必须有自身可靠的双偏振异常才能新增拒绝。
高质量邻站支持与新增关联污染证据冲突时进入隔离；没有邻站支持的数据不算污染票。
邻站支持可用性单独记录。垂直支持不独自推翻目标门的污染证据。

尚未完成：跨方位宽扇区的对象合并、时间证据接入、完整邻站补充策略和五例逐红框
真实降水误删验收。因此四点方案仍未全部完成，不能据此推广。

## 必须更正的效果口径

先前脚本将“DBZH_QC 有限且 QC_ACTION 非 REJECT”称为可见域，这是审计域，**不是网页域**。
实际 `diagnostics/renderer.py` 的 renderer 1.2.0 联合过滤：

- BUSINESS_HARD_REJECT_FLAG_NAMES 对应的原因标志；
- qc-flags-v2 的 QPE_ELIGIBLE_MASK。

本步已修正对照图片、补充 `removed_business_visible_ge10`，并更正上一轮报告。
不能将审计域新增拒绝门数当作截图改善。

## 105 冻结扫描增量试验

同一批四站 29 个扫描、最低仰角，固定已有基线决定，仅追加实验模块。
基线与原始 DBZH、方位、距离一致性检查通过。

|指标|第一轮窗口模块|增加同径向关联|
|---|---:|---:|
|新增 REJECT（全强度）|749|5642|
|审计域新增 REJECT（>=10 dBZ）|435|1642|
|新增 QPE 排除门|0|0|
|实际业务显示域新增剔除门|0|0|

关联门 58110；新增隔离 0。高 RHOHV（>=0.95）及缺测门新增剔除 0。
这些约束检查不等同真实天气误删率为零。

结论：对象关联已成为可执行且可追溯的路径，但触达的新增拒绝仍在原本业务不可用域，
**本轮没有证实网页径向残留得到改善，不进行生产替换**。

## 五例扫描绑定

通过当前 `/api/v1/workspace/cycles/{cycle_id}` 的 radars.scan_id 核对，不根据扫描起始分钟猜测。
这是当前时次的扫描绑定；冻结 QC 仍使用原有输入资产，不能声称替换了当前网页产品。

|北京时间|站|scan_id|审计域新增拒绝 >=10 dBZ|业务图增量|
|---|---|---|---:|---:|
|08:15|Z9598|9ee02e8c-c049-5d7d-8ff2-7c119f99d160|162|0|
|08:35|Z9598|5172b858-0406-5498-89fd-16db363507d1|27|0|
|08:40|Z9598|94ab06ea-7f48-5e97-93ae-0fd8fa9261ed|223|0|
|08:45|Z9598|d12ac95a-4039-5ae7-99f8-6e134da05e7f|424|0|
|08:40|Z9591|5a8d4031-5780-5196-89b1-47e79b648c3b|51|0|

本地汇总页：`runtime/reports/radial-fragments-20260915/linked-cases/index.html`。
服务器结果：`runtime/reports/radial-fragments-20260915/association/summary.json`。
原始证据 NPZ 包含每个目标的父门，后续可以直接回溯实际剩余门。

## 验证与重现

新增关联测试覆盖：跨短缺口、缺口不填值、距离上限、防递归扩张、反射率不一致不关联、
仅关联不继承删除、缺邻站不是负证据、可靠邻站冲突隔离、低 SNR 门保留待审。
新模块测试共 11 项；连同 V7/V6.1 相关测试共 57 项通过。开启关联的实际 Worker 序列化通过。

```sh
python -m rainpulse_algo.radar.qc_engine.fragment_radials \
  --root /frozen --out /output/association --association-distance-m 4000 \
  --profile /workspace/configs/qc/fujian-qc-evidence-graph-v7.yaml \
  --flags /workspace/configs/qc/flag-definitions-v2.yaml
```

后续必须以“业务显示域仍可见”及“仍有 QPE 资格”为目标域，补充对应原始门证据。
不得再将业务域外的审计掩膜改善当作截图残留解决。
