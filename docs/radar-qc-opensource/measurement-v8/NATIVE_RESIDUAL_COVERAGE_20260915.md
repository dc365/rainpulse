# 原生检测未覆盖稀疏残留：实测定位

2026-09-15，105上对Z9598北京08:35与08:45指定扫描最低仰角做只读实验，未部署新质控。

## 距离候选假设排查

08:35取消强回波比例要求：业务可见候选0；加入已有有限离群片段关联后仅2门；允许3km间隔/20%缺口仍仅2门。08:45三种实验均为0门。该探测临时绕过配置验证将强比例设0，仅作原因排查，不能把它直接作为可部署配置。没有处置门或效果提升，不采用此路线。

## 原生 bRopo 实际覆盖

冻结V8 extraction.json显示两扫描原生状态均为unsupported_angular_geometry，calls=0。emitter1/emitter2在指定残留窗口内全为NaN，而非检测后的零分。

启用已存在的注入式最近观测方位匹配（angular_mapping，不插值/不重复观测）：

| 扫描/窗口 | 窗口业务可见门 | 32射线块/宽3：调用数 | 8射线块/宽1：调用数 | 残留窗口实际有分数门 |
|---|---:|---:|---:|---:|
| 08:35，200–280°、>100km、≥10dBZ |6404|0|26|0|
| 08:45，280–330°、>100km、≥10dBZ |1728|0|24|0|

二进制SHA与冻结记录一致。前者状态no_usable_native_support，后者executed；后者仅在其它位置运行，不代表覆盖残留。适配器要求计算块及halo内的观测全部可表示，稀疏区域没有合格矩形块。

## 本轮修复

measurement_v8/workbench.py新增native_original_gate_coverage，记录原始观测覆盖、业务可见≥10dBZ门的已计算/未计算数量；缺失分数不能解释为无干扰。测试包含“原生在其它位置运行而业务覆盖仍0”和“有效零分属于已计算”两个断言场景。

此修复只是避免错误验收，尚未解决残留。后续需要基于原生核心实际访问邻域设计缺测感知适配，并验证缺测不会被当成低反射率制造边缘响应；不能直接以0填充缺测后发布。当前源码未提交，生产配置未改变。

可复现实验：runtime/reports/radial-fragments-20260915/native_mapping_probe.py及native-mapping-probe.json。二者使用冻结29扫描目录与原始C核心。native_residual_probe.py用于检查原始冻结分数。
