# Z9591 10:42、10:48：有界端部参考试验

状态：代码已实现，原始门级阶段回放完成；未切换生产配置，未发布网页产物，不代表整套质控已验收。

## 已证实的残留路径

对象为北京时间 2026-08-28 Z9591 10:42、10:48 最低仰角。线上源资产为 qc-opensource-7.2.0，两个 scan_id 分别为 6abb9e1a-3ef0-579d-aaf2-7ac89bb28645、3e4e5a3b-e43a-574a-9b9b-0e478adc0095。

大量残留门已有 P2 距离模型和 OC1 源相容证据，但位于参考域端部，被双侧参考条件挡住。10:48 还存在大量 SOURCE_FAMILY_UNRESOLVED 门；该类不是本次放开的对象。

## 改动

新配置 fujian-qc-generalization-edge，pipeline qc-opensource-7.2.1，默认旧配置不开启。

只从同一径向、同一个 P2 对象内连续至少 25 km 的双侧参考合格区段出发，向距离端部最多延伸 100 km。每个目标门仍必须满足已有距离模型、OC1 源相容性、无独立天气支撑、无功率/极化冲突且真实有观测。缺测、天气保护、证据冲突、对象边界都会中断路径。不把延伸门再次当作锚点，不跨空洞扩散。

新增门仅隔离、不标记为确认污染；保留原始观测、禁止进入 QPE。新增门级掩膜 P2_BOUNDED_EDGE_REFERENCE_MASK、原因位 4096 和计数。100 km 为两个现有 50 km 参考块的有限试验上限，25 km 为连续锚点试验要求，尚不是经真值标定的普适最优参数。

旧版本默认参数哈希保持不变。7.2.1 配置单独冻结；工人、产物验证兼容新版本。部署文件已准备，但未应用。

## 阶段回放结果

回放使用已保存的 7.2 最低仰角门级证据，不是重新运行整个 Worker。双侧锚点由源相容且未标记 UNBRACKETED_REFERENCE 的门恢复；最终完整 Worker 仍使用 OC1 原生 bracketed_reference_mask。

| 站点/北京时间 | 原 QPE 合格门 | 新增隔离门 | 阶段回放后合格门 |
|---|---:|---:|---:|
| Z9591 10:42 | 172403 | 52698 | 119705 |
| Z9591 10:48 | 266164 | 56681 | 209483 |
| Z9598 10:42 | 未在本次表中统计 | 0 | 不变 |
| Z9598 10:48 | 未在本次表中统计 | 0 | 不变 |

这不是污染清除率或降水准确率。Z9598 零新增隔离只说明本规则未进一步改变这两份对照数据，不能证明旧结果无误删。

对照图位于 runtime/reports/qc-edge-20260917/edge-stage-1042.png、edge-stage-1048.png。依次为原始反射率、7.2 的 QPE 合格门、新规则阶段回放后的 QPE 合格门；不是网页截图，也不是所有可视化图层的替换。10:48 东北侧宽扇区及最外圈仍有残留，不能称问题解决。

## 验证与运行限制

38 项定向机制/集成测试通过，随后新增 1 项完整阶段天气阻断与隔离测试通过（共 39 项测试覆盖）。验证涵盖物理距离上限、非递归延伸、同对象约束、缺测/冲突断点、锚点连续性、无锚点不动、新旧版本实际 Worker 合成产物与幂等性、冻结旧哈希。合成 Worker 有既有 numpy ABI 警告，未导致断言失败。

105 的整日批次仍运行，检查时还有 370 个体扫待算、4 个运行中。完整两体扫旁路回放在开始后退出 137；当时服务器内存使用约 141 GB / 160 GB，无法把它报告为完整回放成功。原工人仍运行。停止追加大体扫并发；当前网页继续使用原发布结果。

## 下一验收关口

1. 内存负载降低后，使用独立受限容器执行完整体扫回放，并比较原始门、最终动作、QPE 资格及正常降水区域。
2. 对剩余 SOURCE_FAMILY_UNRESOLVED 门输出相位、ZDR、RHOHV、SNR 的参考/目标分布，判明是参考区混入天气还是目标真的不相容；不可取消此保护直接整扇区删除。
3. 通过后再协调旧任务排空/版本隔离，切换新配置，重生成诊断图并核对前端资产版本。不要在旧配置任务排队时直接替换 QC 配置，任务有 SHA256 校验。
