# 7.3.4 源边缘关联：统一隔离处置与全体扫验证

候选配置：configs/qc/fujian-qc-source-edge-v1.yaml；源码尚未提交、未发布生产。

## 变化

宽源拟合阶段在宽角支持判定之前，记录目标径向自身的留出功率残差和SNR包络符合性。全部原宽源候选生成后冻结种子，用source_edge单跳关联同距离相邻实测门；不递归传播，不跨几何缺口。保留自身功率、原始50km长条、SNR限制及天气/冲突/数值平台保护。

新候选并入既有BWS_CANDIDATE_MASK，由generalization统一降QI、排除QPE并保留预算复核；不新增确认干扰。BWS_REASON bit128标识边缘支持。默认关闭，旧版本参数哈希不受影响；仅7.3.4身份允许。

## 最低仰角增量回放（105）

|样本（北京时间）|7.3.1高值残留|7.3.4高值残留|
|---|---:|---:|
|Z9591 10:42|2665|341|
|Z9591 10:48|17915|39|
|Z9598 10:42|0|0|
|Z9598 10:48|0|0|

两例Z9598新增隔离为0。先前边缘审计10:48残留31门，加入实际保护及SNR约束后为39门；10:42此前候选340，现为341。不是删除越多越好。
高值域沿用：7.3.1可用、RAW>=45dBZ、距离>=80km；不是污染真值。OC1原始原因复现一致。

## 完整体扫

full_replay.py从原始标准化体扫读取，按旧产物记录的上下文选择和决策截止时刻，运行prepare_qc_inputs及apply_basic_qc的所有阶段/仰角，不复用既有最终QC动作。原始与上下文对象走校验读取。
研究结果留在/tmp/qc734，不发布，不运行QPE/预报。完整运行没有执行生产事件投递与产物发布；合成Worker集成测试单独检查产物与幂等。
旧部署的几何配置目录不可用，因此此次同条件复现中beam/terrain也不可用。这是显式限制，不能把邻站原始文件存在当作可信跨雷达天气证据。

完整运行结果见同目录runtime/reports/qc734-source-edge-20260917/full-{1..4}.json。

## 测试

58项相关测试通过（包括7.3.4实际Worker产物和幂等、旧配置哈希冻结）。新增集成测试先因未支持source_edge配置失败，再实现通过；覆盖窄边缘新增命中、SNR不符、天气/冲突保护。Ruff与git diff --check通过。原有NumPy ABI警告仍在，未新增测试失败。

```sh
algorithms/.venv/bin/python -m pytest algorithms/tests/test_source_edge.py algorithms/tests/test_radial_opening.py algorithms/tests/test_broad_source.py algorithms/tests/test_generalization_integration.py algorithms/tests/test_generalization_p0p2.py -q
```

边界：10:42仍有341个高值残留；无独立真值，不能宣称正常降水无误删。7.3.4仍operational_eligible=false，预算复核不清除。

完整回放4/4已完成，每例11个仰角，耗时分别78.4秒、80.6秒、84.7秒、74.2秒。最低仰角残留341/39/0/0，与增量一致；Z9591两例预算复核保留。正常回波误删尚无真值验收。full-{1..4}.png为完整计算结果对比旧7.3.1的可用反射率原生投影图。

## 正式部署与前端切换

2026-09-17应用户要求提交9dd040d，镜像构建权限修正73aa24b。105四个QC worker及诊断worker已切换7.3.4并健康；控制面默认QC配置同步切换。批次75f75a97-1725-4652-b855-ce810d74bf8c完成8/8雷达体扫、2/2诊断图，状态SUCCEEDED。10:42、10:48网页API均返回7.3.4站点QC图，下载PNG哈希与清单一致；10:48现场PNG已查看。未重算QPE/拼图/预报。证据runtime/reports/qc734-production-20260917。
