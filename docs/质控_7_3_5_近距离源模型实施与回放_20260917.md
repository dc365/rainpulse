# 7.3.5 近距离源模型判定

原因已由105发布的7.3.4逐门数据确认：红框50km内未进入宽源检测，BWS标记为0；不是天气保护导致。旧高值验收只看80km外，未覆盖该区域。

新增near_range_targets开关，明确区分目标和参考域。训练继续使用50km外，目标/保护块排除，近距离自身数据不训练参考。允许已有r>0近距离门通过功率、SNR、极化与宽角一致性判定；不使用近距离长条/边缘规则绕过极化保护。原因bit256保留近距离身份；隔离动作、天气/冲突/平台保护、预算复核不变。旧版默认关闭，参数哈希不变；新配置fujian-qc-near-source-v1.yaml仅7.3.5允许。

## 105最低仰角增量结果

以正式7.3.4为基线，原始OC1原因复现一致，generalization执行正式保护/隔离链路。

|北京时间/站点|红框方向10–50km，≥30dBZ原可用门|候选后剩余|
|---|---:|---:|
|10:42 Z9591（85–180°）|14496|2752|
|10:48 Z9591（280–360°及0–80°）|16605|8311|

以上是按截图方向划定的诊断扇区，不是精确人工标签。Z9598两个时次新增隔离均为0。Z9591全部近距离≥30dBZ可用门20131→7112、16915→8571；远距离高值残留仍341/39，不受新增目标域影响。

已查看10:48三列对照图，小扇区明显稀疏但仍有残留。没有为了清空图形放松极化冲突。候选未经独立天气真值验收，不能将减少门数等同正确删除率；本轮为最低仰角阶段增量回放，未完成全体扫/生产发布。线上仍为7.3.4。

58项组合测试通过，后补形态/边缘不能绕过近距离极化冲突的测试也通过（近距离专项共2项）。覆盖近距离参考隔离、功率异常、天气/冲突保护、远距离输出不变。先运行未支持开关的失败测试，再实现。Ruff通过；原有NumPy ABI警告仍在。

证据runtime/reports/qc735-near-source-20260917/result-{1..4}.json及.json.png；replay.py保存复现入口，输入105 /tmp/qc735/case-{1..4}.zarr。未提交、未部署此候选。
