# 7.3.6 近距离共同源扇区接入

near_sector_consensus已接入宽源模型和统一generalization处置链路，配置fujian-qc-near-sector-v1.yaml；仅7.3.6且near_range_targets启用时允许。旧配置哈希不变。

- 源匹配门须有宽角参考、留出功率残差<=2.5dB及SNR包络匹配。
- scikit-image开运算选出至少3条连续径向、10km连续范围的共同源区域；按几何gap分段，首尾保守断开，不跨缺测。仅处理0–50km。
- 天气、外部冲突、局地增强、数值平台仍有否决权。源形态可补充原极化包络冲突，但仅进入可疑隔离，原TARGET_CONFLICT标记保留；新增bit512说明来源，不新增确认干扰。
- 最终QI、QPE eligibility、可用反射率、预算复核均沿用统一入口；没有只隐藏图片。

62项相关测试通过，含7.3.6实际Worker产物/幂等、旧配置哈希、近距离保护及非递归几何测试。新增集成测试先因不支持开关失败，再实现通过。Ruff与diff检查通过，原有NumPy ABI警告仍在。

完整回放从原始标准化体扫和保存的上下文选择重新计算所有仰角，未发布生产。结果见runtime/reports/qc736-near-sector-20260917/full-{1..4}.json；full-1/2.png是80km内放大对照，左线上7.3.4，右7.3.6完整计算。

需区分：门数减少是诊断覆盖，不是真值召回率。两个Z9598样本不能替代多类型正常降水验收。站点几何配置在保存部署条件下不可用，天气支持存在缺口；因此保留operational_eligible=false和预算复核。

当前代码未提交/部署，线上仍7.3.4。

## 完成结果

4/4完整体扫完成，每个11个仰角，耗时71.8/66.0/67.1/67.2秒。Z9591红框方向10–50km且>=30dBZ剩余347/2529，与阶段回放一致；两个Z9598最低仰角新增隔离0。远距离高值残留341/39不变。预算复核保留。
