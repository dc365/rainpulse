# 几何与逐门证据复核

完成 object_gate_review 模块：将 Py-ART 对象、scikit-image 双侧几何与BWS门级
参考、功率、目标冲突、天气、平台及缺测状态关联。原因位允许重叠，不把缺证据
当成通过。候选是现有BWS候选与几何约束的交集，不宣称产生新的独立污染证据。
输入不变、未发布，也未替换生产7.3.1。

105四例在7.3.2增量剩余域重新运行。统计域仍是原可用、≥45dBZ且≥80km。

- 10:42：1230门残留。339门几何未确认且源参考不可用。通过几何的对象24内，
  891门中1门功率不符、890门目标冲突。891门的同距离参考中828门有一致供体、
  63门供体不足。外部天气/增强/几何冲突输入在本统计域为0。
- 10:48：6558门几何均未双侧确认，其中1580门源参考不可用、4978门目标冲突。
  4978门中3392门供体一致、1348门供体不一致、238门供体不足。
- 两个时次新增可处置门均为0；Z9598对照没有高值域残留。

TARGET_CONFLICT 是现有BWS合并原因，可含SNR、极化及外部冲突，不能仅凭该位
断言是哪一个极化参数。外部冲突为0后，仍需逐参数拆解。

结论：几何证据有效，但不是当前主要判决瓶颈。下一步应按同对象、距离、
PHIDP/ZDR/RHOHV/SNR逐项分析包络偏离与分布尾部，检查供体选择；不能简单扩大
5–95分位包络或整对象删除。缺少独立天气真值时不能把所有尾部都称为污染。

10项相关测试通过（新增逐门保护1项、边界5项、对象4项），Ruff通过。
新增测试先运行缺模块红灯，再实现通过。原有numpy ABI警告仍存在。

复现：105候选环境运行 scripts/audit_qc_pyart_objects.py --case 1 --boundaries。
输出 runtime/reports/qc732-distance-polar-20260917/gate-review-{1,2,3,4}.json。
