# 源模型边缘关联：四样本回放

新增独立研究模块 source_edge.py；尚未接入生产仲裁、提交或发布。生产仍为7.3.1。

窄分支失败原因是两侧正反差和连续性不成立。新假设：自身有独立距离留出拟合的残留径向，可与同距离相邻已识别源候选关联。它不要求目标高于两侧，不把缺测当低值。仅单跳、角差<=1.5°，无几何断裂；自身残差<=2.5 dB，原始>35 dBZ连续50 km，天气/冲突保护强制显式传入。冻结种子不递归扩张。

|样本（北京时间）|7.3.3高值残留|新增候选高值门|候选应用后残留|全部新增可用门候选|
|---|---:|---:|---:|---:|
|Z9591 10:42|341|1|340|1|
|Z9591 10:48|1596|1565|31|1732|
|Z9598 10:42|0|0|0|0|
|Z9598 10:48|0|0|0|0|

高值统计域：此前可用且未被7.3.3候选隔离、原始>=45 dBZ、距离>=80 km。不是人工真值。东侧80.83°长条1317门命中1316门。10:42的319.84°291门没有相邻源种子，未强行扩张。

四样本均仅最低仰角。回放缺独立邻站/时间天气证据，传入现有BWS保护/数值平台位，但不能宣称无误删；这些数值是候选上界。单侧相邻关系也不证明同源，下一步应完整流水线验证天气保护和SNR/极化冲突，才能升级仲裁或部署。图片是原生ray-index×range研究图，不是生产地图。

验证：先运行测试得到模块缺失错误，再实现；4项相关测试通过，Ruff通过。覆盖非递归、缺测种子、天气/冲突、目标功率不符、几何断裂。未进行全仓兄弟模块扫描，本轮为独立候选试验。

复现：105缓存最低仰角 /tmp/morph-{1..4}.zarr，运行 runtime/reports/qc-source-edge-20260917/audit_edge.py --case N（脚本保留实验路径）；需候选7.3.3代码、broad_source_probe.py和scikit-image0.26.0。probe仅在宽角判据前记录已有留出拟合残差，不修改宽源候选条件。输出edge-N.json/png。正式模块不依赖该probe。
