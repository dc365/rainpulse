# 近距离规则扇区：共同源形态候选

7.3.5剩余区域复核：10:42的2752门中2750门功率残差<=2.5dB；10:48的8311门中8261门符合。全部原因275（近距离+参考+宽角+目标冲突），说明不是源拟合失败，而是完整逐门匹配未通过。不能只凭规则扇形认定污染。

研究模块near_sector：使用已有宽角源参考、自身功率残差和留出SNR包络均合格的实测门，scikit-image opening(3 rays,10km)提取共同源区域。按几何gap分段、不跨缺测、不递归扩张，仅r>0且<50km。天气、局地增强、外部天气、几何异常、数值平台仍阻止候选。允许形态与源规律补充原极化包络冲突，但只生成可疑候选，未接生产处置。

|红框方向，10–50km且>=30dBZ|7.3.5后|共同源形态候选后|
|---|---:|---:|
|Z9591 10:42|2752|347|
|Z9591 10:48|8311|2529|

全域新增可用门候选3261/6070；Z9598两个对照0。均是最低仰角增量实验，天气独立证据仍缺乏，不能称为真值召回率或无误删。仍有残留，未继续放宽阈值。固定3条径向是当前候选约束，不宣称跨扫描分辨率已标定；首尾接缝保守断开。

单元测试先缺模块失败再通过，覆盖正例、缺测、保护、几何断裂、50km边界，Ruff通过。回放所用near_sector_probe_model.py仅导出现有拟合SNR包络匹配，用于候选审计，不修改原BWS候选。证据及复现脚本：runtime/reports/qc-near-sector-20260917/。sector-1/2.png为80km近距离放大对照（左7.3.5，右本候选）。

状态：研究代码未提交/部署，线上仍7.3.4。下一步应统一接入候选原因/隔离预算并完成全体扫与正常降水对照，不能把研究候选图当成线上结果。
