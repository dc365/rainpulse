请实施《雷达质控优化：总体设计与实施交接》。目标仓库是 rainpulse-nowcast，先读 AGENTS.md、PROJECT_MEMORY.md 和交接文档，并保护当前用户未提交工作。

按文档顺序完成 A1 至 A8，每个工作包先补能失败的回归，再实现并验证。保留旧 profile 语义，新增 evidence-v2。重点是邻线扩杀、上层缺测、统一 veto 裁决、时间上下文窗口、圆周方位索引、重复校验/计算、存储与审计。不要只降低阈值，不要移除 Hybrid/mosaic 的硬拒绝门禁。

A 阶段完成后，按 B1 至 B3 实现诊断和数据工具；无真实资料时保持负证据与新增分类硬判关闭。C 阶段按明确数据门槛执行，不猜物理系数、不伪造静态图、人工真值或业务准入。

本次任务要求代码、测试、文档和可复现基准。提交、推送、部署、全历史回算或删除派生数据，只在我另行明确授权时执行。

交付时逐项列 A/B/C 完成状态，给实际测试输出、科学差异、性能 JSON、版本及上下文指纹。报告必须区分软件通过、影子回放通过、真实气象效果通过。不要把已有 60 项合成测试通过当作新功能验证，也不要把函数级加速比称为整链路加速。
