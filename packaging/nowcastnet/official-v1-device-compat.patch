diff --git a/code/nowcasting/layers/utils.py b/code/nowcasting/layers/utils.py
index 0561b7a..c7370ab 100644
--- a/code/nowcasting/layers/utils.py
+++ b/code/nowcasting/layers/utils.py
@@ -9,2 +9,2 @@ def make_grid(input):
-    xx = torch.arange(0, W).view(1, -1).repeat(H, 1).cuda()
-    yy = torch.arange(0, H).view(-1, 1).repeat(1, W).cuda()
+    xx = torch.arange(0, W, device=input.device).view(1, -1).repeat(H, 1)
+    yy = torch.arange(0, H, device=input.device).view(-1, 1).repeat(1, W)
diff --git a/code/nowcasting/models/model_factory.py b/code/nowcasting/models/model_factory.py
index cd8ebc1..5a0f36a 100644
--- a/code/nowcasting/models/model_factory.py
+++ b/code/nowcasting/models/model_factory.py
@@ -24 +24 @@ class Model(object):
-        stats = torch.load(self.configs.pretrained_model)
+        stats = torch.load(self.configs.pretrained_model, map_location=self.configs.device)
diff --git a/code/nowcasting/models/nowcastnet.py b/code/nowcasting/models/nowcastnet.py
index 54782c8..14f52fc 100644
--- a/code/nowcasting/models/nowcastnet.py
+++ b/code/nowcasting/models/nowcastnet.py
@@ -23 +23 @@ class Net(nn.Module):
-        self.grid = make_grid(sample_tensor)
+        self.register_buffer("grid", make_grid(sample_tensor), persistent=False)
@@ -45 +45 @@ class Net(nn.Module):
-            last_frames = warp(last_frames, motion_[:, i], grid.cuda(), mode="nearest", padding_mode="border")
+            last_frames = warp(last_frames, motion_[:, i], grid, mode="nearest", padding_mode="border")
@@ -56 +56 @@ class Net(nn.Module):
-        noise = torch.randn(batch, self.configs.ngf, height // 32, width // 32).cuda()
+        noise = torch.randn(batch, self.configs.ngf, height // 32, width // 32, device=frames.device)
